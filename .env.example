# -----------------------------------------------------------------------------
# Supabase env — copy to `.env.local`. Do not commit `.env.local`.
# This app requires both `VITE_*` variables; there is no browser-local data mode.
# Restart `npm run dev` after any change.
# -----------------------------------------------------------------------------

SUPABASE_PROJECT_NAME=prodsystem12345
SUPABASE_PROJECT_REF=letimvijoajcwqspistb

VITE_SUPABASE_URL=https://letimvijoajcwqspistb.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=

# For psql / CLI only — not read by Vite
DATABASE_URL=postgresql://postgres:YOUR_DB_PASSWORD@db.letimvijoajcwqspistb.supabase.co:5432/postgres
